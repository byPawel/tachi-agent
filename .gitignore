node_modules/
dist/
coverage/
*.log
.env
.env.local
.DS_Store

# dokoro runtime data (memory DB the dokoro server writes into cwd)
dokoro/
.focus-sessions/

# superpowers planning docs — local working files, not tracked
docs/superpowers/plans/

# planner accumulator cache (written into cwd by the planner tools)
.plan-cache/

# context-inspector JSONL output (written into cwd when TACHI_CONTEXT_INSPECT is on)
.tachi/

# Claude Code local state (worktrees hold nested git checkouts; settings are per-machine)
.claude/worktrees/
.claude/settings.local.json

# remember-plugin session buffers
.remember/
.agents/
